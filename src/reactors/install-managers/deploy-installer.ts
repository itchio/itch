export default async function deployInstaller() {
  // battle plan:
  // move existing install folder to temp location
  // kick off installer
  // walk when installer's done
  // merge immortals into install folder
  // wipe old folder
}
