// this session doesn't cache - see preboot for setup
export const NET_PARTITION_NAME = "itch-zone";

// this session accepts certificates for 127.0.0.1
export const LOOPBACK_PARTITION_NAME = "itch-loopback";

// in milliseconds
export const NET_TIMEOUT_MS = 10 * 1000;
