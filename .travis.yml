language: node_js
node_js:
  - "4.1"
before_install:
  - npm install -g gulp
  - npm install
  - (cd app && npm install)
  - gulp all
  - (cd spec/chrome && npm install)
  - npm install -g 'electron-prebuilt@itchio/electron-prebuilt#exit-code'
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
script:
  - (cd spec/chrome && electron . --ci)
